// Define un componente funcional llamado Contact
function Contact() {
     // Retorna el contenido que se renderizará en pantalla
  return (
    <div>
        {/* Título que se mostrará en la página */}
      <h1>Contact</h1>
    </div>
  );
}
// Exporta el componente para poder usarlo en otros archivos (por ejemplo en App.js)
export default Contact;